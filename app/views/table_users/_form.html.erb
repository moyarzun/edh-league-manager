<%= form_with(model: table_user) do |form| %>
  <% if table_user.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(table_user.errors.count, "error") %> prohibited this table_user from being saved:</h2>

      <ul>
        <% table_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :table_id, style: "display: block" %>
    <%= form.text_field :table_id %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
